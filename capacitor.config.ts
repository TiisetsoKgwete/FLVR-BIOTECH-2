import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flvrbiotech.app',
  appName: 'FLVR BIOTECH',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
